# 설교 카드 자동 생성 기능 — 배포 안내

## 무슨 기능인가요
설교 영상 카드에 나오던 유튜브 화면 그대로 대신, **목사님 사진 + 자동 그라데이션 배경 + 설교
제목**을 서버가 자동으로 합성해서 보여줍니다. 유튜브에 새 영상이 올라올 때마다:
- "주일예배"와 날짜(8자리 숫자)는 자동으로 빠지고
- 성경 구절("사도행전 19장 1~7절")과 설교 제목이 자동으로 분리되고
- 색상 5종·목사님 사진 3장·사진 좌우 위치가 영상마다 자동으로 달라져서
- 한 번 만든 이미지는 저장해뒀다가 재사용합니다(같은 영상을 다시 열 때마다 새로 안 만듦)

## 새로 추가된 파일 (반드시 다 있어야 동작합니다)

| 파일 | 저장소 경로 | 크기 |
|---|---|---|
| `utils/sermonPoster.js` | `utils/sermonPoster.js` | 핵심 생성 로직 |
| `utils/fonts/NotoSansKR-Black.otf` | `utils/fonts/NotoSansKR-Black.otf` | 약 17MB (한글 굵은 글씨체) |
| `utils/assets/sermon-card-photos/pastor-1.png` | 〃 | 목사님 사진(배경 제거됨) |
| `utils/assets/sermon-card-photos/pastor-2.png` | 〃 | 〃 |
| `utils/assets/sermon-card-photos/pastor-3.png` | 〃 | 〃 |

⚠️ **폰트 파일이 17MB로 좀 큽니다.** GitHub 저장소에 커밋하실 때 시간이 조금 걸릴 수 있어요. 문제
없이 커밋되니 걱정 안 하셔도 되지만, 업로드가 오래 걸려도 정상입니다.

## 수정된 파일

| 파일 | 저장소 경로 | 내용 |
|---|---|---|
| `public/js/main.js` | `public/js/main.js` | 설교 카드가 자동생성 이미지를 쓰도록 변경 |
| `public/css/style.css` | `public/css/style.css` | 이제 필요 없어진 카드 꾸밈 스타일 정리 |
| `public/admin/index.html` | `public/admin/index.html` | 설교 카드용 사진 추가 업로드 UI |
| `public/admin/js/admin.js` | `public/admin/js/admin.js` | 사진 추가/삭제 관리 로직 |
| `routes/api.js` | `routes/api.js` | 카드 자동생성 API 추가 |
| `package.json` | `package.json` | (참고용, 새 의존성 없음 — sharp는 이미 있음) |

## 배포 후 확인할 것

1. `npm install` 다시 실행 (혹시 모를 의존성 재설치)
2. 홈페이지 → 설교 영상 섹션 → 카드들이 "목사님 사진 + 그라데이션 + 제목" 형태로 나오는지 확인
3. 영상 카드를 눌러서 재생이 잘 되는지 확인 (이미지만 바뀌었을 뿐, 클릭하면 재생되는 기능은 그대로예요)
4. 만약 카드가 예전처럼 유튜브 화면 그대로 나온다면, `utils/sermonPoster.js`나 폰트 파일이 제대로
   안 올라갔을 가능성이 높아요 — 이 경우 자동으로 예전 방식(유튜브 썸네일)으로 대체되도록 안전장치를
   넣어뒀어서 사이트가 깨지지는 않습니다.

## 사진 더 추가하고 싶으실 때

관리자 페이지 → 기본 정보 → '설교 영상 섹션' 카드 → **'설교 카드용 목사님 사진'**에서 사진을
추가로 올리시면, 기본 3장과 함께 자동으로 섞여서 사용됩니다.

⚠️ **주의하실 점**: 여기서 직접 올리시는 사진은 **배경이 제거되지 않고 원본 그대로** 사용됩니다
(배경 제거는 사람이 직접 처리해야 하는 작업이라 자동화하지 못했어요). 지금 들어있는 기본 사진
3장처럼 깔끔한 느낌을 원하시면, 사진을 저(Claude)에게 보내주시면 같은 방식으로 배경을 제거해서
다시 드릴 수 있어요. 그게 아니라면 그냥 원본 사진 그대로도 나쁘지 않게 나올 수 있습니다 — 배경이
너무 복잡하지 않은 사진 위주로 골라서 올리시는 걸 추천드려요.

## 참고 — 찬양 카드는 이번에 손대지 않았어요
이번 자동화는 '설교 영상'에만 적용했습니다. '찬양' 카드는 지난번 만든 방식(썸네일 위에 색상
오버레이 + 제목) 그대로 유지됩니다. 찬양도 같은 방식으로 바꾸고 싶으시면 말씀해주세요.
